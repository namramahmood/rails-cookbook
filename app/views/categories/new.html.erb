<h1>New Category</h1>

<%= simple_form_for @category, html: { data: { turbo: false } } do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>

<%= link_to 'Back to Categories', categories_path %>
